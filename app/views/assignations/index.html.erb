<h1>Listando Asignaciones</h1>

<table>
  <tr>
    <th>Usuario</th>
    <th>Categor&iacute;a</th>
    <th></th>
  </tr>

<% @assignation_user.each do |assignation| %>
  <tr>
    <td><%= assignation.technician.firstname %></td>
    <td><%= assignation.category.name %></td>
    <td><%= link_to 'Eliminar', assignation, confirm: '¿Est&aacute seguro?', method: :delete %></td>
  </tr>
<% end %>


</table>

<br />

<%= link_to 'Nueva Asignación', new_assignation_path %>
